/* import React from 'react';
import './ListMap.css';

export default function ListMap() {
  
  function toggle(mode) {
    if (mode == list) {
        return <List />
    } else {
        return <Map />
    }
  }

  return (
      <div>
          <button type="button" onClick={function (){toggle(list)}}>
            List
          </button>
          <button type="button" onClick={function (){toggle(map)}}>
            Map
          </button>
      </div>
  ) */